import React, { useEffect, useState } from "react";
import { View, Text, StyleSheet, ScrollView } from "react-native";
import { LineChart } from "react-native-chart-kit"; // Or use BarChart depending on your preference
import { fetchWaterIntakeForWeek } from "../../services/firebase/waterService"; // Import the function to fetch water intake

const WaterIntakeChart = ({ userId, date }) => {


}
export default WaterIntakeChart;
